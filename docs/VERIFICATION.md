# v0.4.0 검증 기록 (2026-09-18)

- Windows / Python 3.13: pytest **68개 통과** (20.90초).
- 완료 시 cleanup 요청, 삭제 확인 전 ID 보존, 삭제 확인 후 정리 및 반복 호출, 소유자/ID 불일치 거부, STOP/BLOCKED/빈 완료 근거에서 삭제 정리 거부 검증.
- 완료 후 새 예약을 붙이려면 명시적으로 완료 상태를 해제해야 함을 검증. 프로젝트 완료 근거와 목표 파일 보존 확인.
- 배포 manifest·스킬·진입점 검사 통과. 독립 읽기 검토에서 추가 수정 사항 없음.
- 실제 진행 중 프로젝트를 완료로 가장하거나 실예약을 시험 삭제하지 않았습니다. 호스트 예약 삭제는 완료 시 제공된 도구로 수행하고 성공 여부를 확인해야 합니다.

## v0.3.0 검증 기록 (2026-09-17)

- Windows / Python 3.13: `pytest -q` **63개 통과** (25.50초). 실제 모델 실행 없이 검증.
- 공식 `codex app-server` 초기화와 `account/rateLimits/read`를 실제 ChatGPT 로그인 환경에서 호출하여 `codex` 버킷 및 사용 가능 응답 확인. 모델 turn/구매/reset 호출 없음.
- 소진된 두 창의 가장 늦은 시각 선택, 비소진 창 무시, 알 수 없는 버킷, 과거/누락/비정상 초기화 시각, 거대한 정수, 타임아웃, 중지, 재시작 후 대기, 정보 조회 실패 시 모델 실행 금지, 앱 대기와 재개 검증.
- 할당량 조회 중 최대 실행 시간이 만료되면 새 모델 작업을 시작하지 않는 회귀 테스트 통과.
- 배포 manifest/스킬 참조/진입점 4개 검사, wheel 빌드 통과.
- 독립 읽기 검토에서 숫자 범위와 조회 후 실행 시간 재확인 문제를 발견해 수정. 보안 재검토에서 추가 차단 결함 없음.

미검증: 실제 할당량을 소진한 뒤 몇 시간/며칠 후 회복되는 전체 장기 주기, 앱 호스트 예약 시각 조정 및 소진 중 wakeup 동작. 앱 helper 검증은 호스트 스케줄러 보장이 아닙니다. Windows/Linux 및 Python 3.10/3.13 CI는 해당 커밋의 GitHub Actions 결과를 확인하세요.

## 이전 v0.2.0 검증 기록

로컬 검증 환경: Windows, Python 3.13, Codex CLI 0.154.0.

- pytest: **40개 통과**. 실제 모델 호출 없이 가짜 Codex 자식 프로세스로 검증했습니다.
- 포함 범위: 한도 오류 후 대기·성공·완료, 저장된 대기 시각 복원, 백그라운드 중복 실행 방지, 정상/즉시 중지, 자식 프로세스 종료, 실행 중 추가 지시 보존, 인증 확인, 출력 상한, 실행 횟수 제한, 앱 소유권·중지·자동화 연결 보호.
- 추가 회귀 검증: 대상 프로젝트의 Python 패키지가 supervisor를 가로채지 못함, 시작 취소 시 백그라운드 프로세스 정리.
- plugin-creator manifest 검증과 skill-creator 스킬 검증 통과.
- 배포용 manifest·스킬 참조·실행 진입점 4개 검증 통과.
- Python wheel 빌드와 MIT LICENSE 포함 확인.
- 실제 사용자 환경에서 Codex CLI의 ChatGPT 로그인 상태 확인. 인증 정보는 읽거나 배포하지 않았습니다.

미검증 범위: 실제 계정의 할당량 소진부터 복구까지 이어지는 장시간 실행, 호스트 앱 heartbeat의 실제 한도 복구 주기, macOS 실행. Linux 및 다른 Python 버전은 GitHub CI 결과를 별도로 확인해야 합니다.

앱 도우미 테스트 통과는 호스트 스케줄러의 자동 재시도 보장을 뜻하지 않습니다. 앱 스케줄러 기능 제공 여부와 실행 조건은 사용자 환경에 따라 달라집니다.
